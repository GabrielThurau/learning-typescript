const activeUsers: string[] = [];

activeUsers.push("Gabe");

type Point = {
    x: number,
    y: number
}


const coordinates: Point[] = [];

coordinates.push({ x: 12, y: 14 });

console.log(coordinates);

const board: string[][] = [["X", "O", "X"], ["X", "O", "X"], ["X", "O", "X"]];

console.log(board);