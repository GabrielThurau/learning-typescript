let movieTitle: string = "Four Lions";

function square(num: number) {
    return num * num;
}

function random(num: number) {
    if (Math.random() < 0.5) {
        return num.toString();
    }
    return num;
}
